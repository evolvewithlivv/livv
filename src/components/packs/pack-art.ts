import type { PackGrade } from "@/lib/packs";

/* Canonical visual order: Spark → Rise → Apex (gold) → Signal. */
export const PACK_ART: Record<PackGrade, string> = {
  1: "/packs/spark.png",
  2: "/packs/rise.png",
  3: "/packs/apex.png",
  4: "/packs/signal.png",
};
