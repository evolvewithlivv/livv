import { DEFAULT_ACCENT,ACCENTS,applyAppearance,type Identity,type Appearance } from "./identity";
import { getAnonSessionState } from "./supabase/anon-session";
import { clearLocalCloudSyncedState,markSigningOut } from "./supabase/cloud-state";
import { getSupabaseBrowserClient,isSupabaseConfigured } from "./supabase/client";
export type AuthProvider="email";
export type Account={id:string;provider:AuthProvider;email?:string;passwordHash?:string;displayName:string;username:string;usernameLocked:boolean;photo:string|null;accent:string;appearance:Appearance;tier:Identity["tier"];theme:Identity["theme"];embers:number;bio:string;goal?:string;createdAt:number;lastLoginAt:number};export type Session={accountId:string;signedInAt:number};
const ACCOUNTS_KEY="livv-accounts-v1",SESSION_KEY="livv-session-v1",USERNAMES_KEY="livv-usernames-v1",IDENTITY_KEY="livv-identity-v1";
function hash(s:string){let h=0;for(let i=0;i<s.length;i++)h=(Math.imul(31,h)+s.charCodeAt(i))|0;return`h${Math.abs(h)}`;}export function loadAccounts():Account[]{if(typeof window==="undefined")return[];try{const raw=window.localStorage.getItem(ACCOUNTS_KEY);if(!raw)return[];const parsed=JSON.parse(raw)as Account[];return Array.isArray(parsed)?parsed:[];}catch{return[];}}function saveAccounts(accounts:Account[]){window.localStorage.setItem(ACCOUNTS_KEY,JSON.stringify(accounts));}function loadUsernameMap():Record<string,string>{if(typeof window==="undefined")return{};try{const raw=window.localStorage.getItem(USERNAMES_KEY);return raw?(JSON.parse(raw)as Record<string,string>):{};}catch{return{};}}function saveUsernameMap(map:Record<string,string>){window.localStorage.setItem(USERNAMES_KEY,JSON.stringify(map));}export function isUsernameAvailable(username:string,exceptAccountId?:string){const clean=normalizeUsername(username);if(clean.length<3)return false;const owner=loadUsernameMap()[clean];return!owner||owner===exceptAccountId;}export function normalizeUsername(raw:string){return raw.toLowerCase().replace(/^@/,"").replace(/[^a-z0-9_]/g,"").slice(0,24);}export function suggestUsername(displayName:string,_provider:AuthProvider="email"){const base=normalizeUsername(displayName)||"member";const candidate=base.slice(0,18)||"member";if(isUsernameAvailable(candidate))return candidate;for(let i=1;i<99;i++){const next=`${candidate}${i}`;if(isUsernameAvailable(next))return next;}return`m${Date.now().toString(36).slice(-6)}`;}export function getSession():Session|null{if(typeof window==="undefined")return null;try{const raw=window.localStorage.getItem(SESSION_KEY);if(!raw)return null;return JSON.parse(raw)as Session;}catch{return null;}}export function isSignedInLocal(){const session=getSession();return Boolean(session&&loadAccounts().find(a=>a.id===session.accountId));}export function isSignedIn(){return isSignedInLocal();}export function getCloudUserId():string|null{const cloud=getAnonSessionState();return cloud.status==="ready"&&cloud.userId?cloud.userId:null;}export async function resolveHomeAccess():Promise<"ok"|"deny">{if(typeof window==="undefined")return"deny";return isSignedInLocal()?"ok":"deny";}export function getCurrentAccount():Account|null{const session=getSession();if(!session)return null;return loadAccounts().find(a=>a.id===session.accountId)||null;}
function writeIdentityFromAccount(account:Account){const identity:Identity={displayName:account.displayName,username:account.username,bio:account.bio,goal:account.goal||"",photo:account.photo,tier:account.tier,theme:account.theme,appearance:account.appearance,accent:account.accent,embers:account.embers};window.localStorage.setItem(IDENTITY_KEY,JSON.stringify(identity));applyAppearance(account.appearance,account.accent);window.dispatchEvent(new Event("livv-identity"));}function setSession(accountId:string){window.localStorage.setItem(SESSION_KEY,JSON.stringify({accountId,signedInAt:Date.now()}));window.dispatchEvent(new Event("livv-auth"));}
export function signOut(){if(typeof window==="undefined")return;markSigningOut();if(isSupabaseConfigured()){const client=getSupabaseBrowserClient();if(client)void client.auth.signOut({scope:"local"}).catch((error:unknown)=>console.warn("[auth] Supabase sign-out failed",error)).finally(()=>{clearLocalCloudSyncedState();});else clearLocalCloudSyncedState();}else clearLocalCloudSyncedState();window.dispatchEvent(new Event("livv-auth"));}
export function setCurrentAccountEmail(email:string){if(typeof window==="undefined")return;const session=getSession();if(!session)return;const accounts=loadAccounts();const idx=accounts.findIndex(a=>a.id===session.accountId);if(idx<0)return;accounts[idx]={...accounts[idx],email:email.trim().toLowerCase()||undefined};saveAccounts(accounts);window.dispatchEvent(new Event("livv-auth"));}
export function claimUsername(accountId:string,username:string){return changeUsername(username,accountId);}
export function changeUsername(username:string,accountId?:string){const session=getSession();const id=accountId||session?.accountId;if(!id)throw new Error("You need to be signed in");const clean=normalizeUsername(username);if(clean.length<3)throw new Error("Username must be at least 3 characters");if(!isUsernameAvailable(clean,id))throw new Error("That username is already taken");const accounts=loadAccounts();const idx=accounts.findIndex(a=>a.id===id);if(idx<0)throw new Error("Account not found");const old=accounts[idx].username;const map=loadUsernameMap();if(old)delete map[old];map[clean]=id;saveUsernameMap(map);accounts[idx]={...accounts[idx],username:clean,usernameLocked:true};saveAccounts(accounts);writeIdentityFromAccount(accounts[idx]);return accounts[idx];}
export function syncAccountFromIdentity(identity:Identity){const session=getSession();if(!session)return;const accounts=loadAccounts();const idx=accounts.findIndex(a=>a.id===session.accountId);if(idx<0)return;accounts[idx]={...accounts[idx],displayName:identity.displayName,bio:identity.bio,goal:identity.goal,photo:identity.photo,accent:identity.accent,appearance:identity.appearance,tier:identity.tier,theme:identity.theme,embers:identity.embers,username:accounts[idx].username};saveAccounts(accounts);}
export async function syncIdentityToCloud(identity:Identity){if(typeof window==="undefined"||!isSupabaseConfigured())return false;const client=getSupabaseBrowserClient();if(!client)return false;const{data,error}=await client.auth.getUser();if(error||!data.user||data.user.is_anonymous)return false;const{error:updateError}=await client.from("profiles").update({display_name:identity.displayName,bio:identity.bio,goal:identity.goal,photo_url:identity.photo,accent:identity.accent,appearance:identity.appearance,tier:identity.tier,embers:identity.embers}).eq("id",data.user.id);if(updateError){console.warn("[LIVV profile] cloud identity write deferred",updateError);return false;}return true;}
export async function signUpWithProvider(input:{provider:AuthProvider;displayName:string;username:string;email?:string;password?:string}){await delay(400);const username=normalizeUsername(input.username);if(!isUsernameAvailable(username))throw new Error("That username is taken");if(username.length<3)throw new Error("Username must be at least 3 characters");if(!input.email)throw new Error("Email required");const exists=loadAccounts().some(a=>a.provider==="email"&&a.email===input.email?.toLowerCase());if(exists)throw new Error("An account with this email already exists. Sign in instead.");const account:Account={id:`acc_${Date.now()}_${Math.random().toString(36).slice(2,8)}`,provider:"email",email:input.email.toLowerCase(),passwordHash:input.password?hash(input.password):undefined,displayName:input.displayName.trim()||"Member",username,usernameLocked:true,photo:null,accent:DEFAULT_ACCENT,appearance:"dark",tier:"spark",theme:"ember",embers:0,bio:"",goal:"",createdAt:Date.now(),lastLoginAt:Date.now()};const accounts=loadAccounts();accounts.push(account);saveAccounts(accounts);const map=loadUsernameMap();map[username]=account.id;saveUsernameMap(map);setSession(account.id);writeIdentityFromAccount(account);return account;}
export async function completeDeviceOnboarding(input:{displayName:string}):Promise<{account:Account;isNew:boolean}>{await delay(200);if(typeof window==="undefined")throw new Error("Onboarding requires a browser");if(!isSupabaseConfigured())throw new Error("LIVV authentication is temporarily unavailable. Please verify your email and try again.");const client=getSupabaseBrowserClient();if(!client)throw new Error("LIVV authentication is temporarily unavailable. Please verify your email and try again.");const{data,error}=await client.auth.getUser();if(error||!data.user||data.user.is_anonymous)throw new Error("Please verify your email before entering LIVV.");const displayName=input.displayName.trim()||"Member";const profileResult=await client.from("profiles").select("display_name,bio,goal,photo_url,accent,appearance,tier,embers").eq("id",data.user.id).maybeSingle();const cloudProfile=profileResult.data;if(profileResult.error)console.warn("[auth] profile hydration deferred",profileResult.error);if(cloudProfile){const existingLocal=loadAccounts().find(a=>a.id===`supabase_${data.user.id}`);if(existingLocal){const hydrated={...existingLocal,displayName:cloudProfile.display_name||existingLocal.displayName,bio:cloudProfile.bio||"",goal:cloudProfile.goal||"",photo:cloudProfile.photo_url||null,accent:ACCENTS.includes(cloudProfile.accent)?cloudProfile.accent:existingLocal.accent,appearance:cloudProfile.appearance==="light"||cloudProfile.appearance==="system"||cloudProfile.appearance==="dark"?cloudProfile.appearance:existingLocal.appearance,tier:cloudProfile.tier||existingLocal.tier,embers:Number.isFinite(Number(cloudProfile.embers))?Number(cloudProfile.embers):existingLocal.embers};const accounts=loadAccounts().map(a=>a.id===existingLocal.id?hydrated:a);saveAccounts(accounts);writeIdentityFromAccount(hydrated);}}if(isSignedInLocal()&&cloudProfile){const existing=getCurrentAccount();if(existing){const accounts=loadAccounts();const idx=accounts.findIndex(a=>a.id===existing.id);if(idx>=0){accounts[idx]={...accounts[idx],displayName,lastLoginAt:Date.now(),email:data.user.email?.toLowerCase()||accounts[idx].email};saveAccounts(accounts);writeIdentityFromAccount(accounts[idx]);return{account:accounts[idx],isNew:false};}}}const existingBySupabaseId=loadAccounts().find(a=>a.id===`supabase_${data.user.id}`);if(existingBySupabaseId){setSession(existingBySupabaseId.id);writeIdentityFromAccount(existingBySupabaseId);return{account:existingBySupabaseId,isNew:false};}const username=suggestUsername(displayName,"email");const account:Account={id:`supabase_${data.user.id}`,provider:"email",email:data.user.email?.toLowerCase(),displayName,username,usernameLocked:true,photo:null,accent:DEFAULT_ACCENT,appearance:"dark",tier:"spark",theme:"ember",embers:0,bio:"",goal:"",createdAt:Date.now(),lastLoginAt:Date.now()};const accounts=loadAccounts();accounts.push(account);saveAccounts(accounts);const map=loadUsernameMap();map[username]=account.id;saveUsernameMap(map);setSession(account.id);writeIdentityFromAccount(account);return{account,isNew:true};}
export async function signInWithEmail(email:string,password:string){await delay(350);const account=loadAccounts().find(a=>a.provider==="email"&&a.email===email.toLowerCase());if(!account||account.passwordHash!==hash(password))throw new Error("Email or password is wrong");account.lastLoginAt=Date.now();saveAccounts(loadAccounts().map(a=>a.id===account.id?account:a));setSession(account.id);writeIdentityFromAccount(account);return account;}function delay(ms:number){return new Promise(r=>setTimeout(r,ms));}
