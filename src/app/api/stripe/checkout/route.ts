import { NextRequest, NextResponse } from "next/server";
import { appUrl, getStripe, priceIdForTier, type PaidTier } from "@/lib/stripe-server";
import { GRADE_META, type PackGrade } from "@/lib/packs";
import { PACK_SHOP } from "@/lib/pack-shop";
import { getVerifiedSupabaseUser, isSupabaseServerConfigured } from "@/lib/supabase/server-auth";
export const runtime="nodejs";
const PAID:PaidTier[]=["rise","apex","circle"],GRADES:PackGrade[]=[1,2,3,4],MAX_BODY_BYTES=16*1024;
function json(data:unknown,init?:ResponseInit){return NextResponse.json(data,{...init,headers:{"Cache-Control":"no-store",...(init?.headers||{})}});}
export async function POST(req:NextRequest){try{const stripe=getStripe();if(!stripe)return json({error:"Stripe is not configured."},{status:503});let livvUserId:string|null=null,verifiedEmail:string|null=null;if(isSupabaseServerConfigured()){const verified=await getVerifiedSupabaseUser(req);if(!verified)return json({error:"Sign-in required for checkout."},{status:401});if(verified.isAnonymous)return json({error:"Verify your email before checkout."},{status:403});livvUserId=verified.id;verifiedEmail=verified.email;}const contentLength=Number(req.headers.get("content-length")||0);if(contentLength>MAX_BODY_BYTES)return json({error:"Checkout request too large"},{status:413});const rawBody=await req.text();if(new TextEncoder().encode(rawBody).byteLength>MAX_BODY_BYTES)return json({error:"Checkout request too large"},{status:413});let body:{kind?:string;grade?:number;qty?:number;tier?:string;email?:string;username?:string};try{body=JSON.parse(rawBody);}catch{return json({error:"Invalid JSON"},{status:400});}const usernameMeta=(body.username||"").slice(0,64),customerEmail=isSupabaseServerConfigured()?(verifiedEmail||undefined):typeof body.email==="string"&&body.email.includes("@")?body.email.trim().toLowerCase():undefined,base=appUrl();if(body.kind==="pack"){const grade=body.grade as PackGrade;if(!GRADES.includes(grade))return json({error:"Invalid pack"},{status:400});const qty=Math.max(1,Math.min(10,Number(body.qty)||1)),shop=PACK_SHOP[grade];const session=await stripe.checkout.sessions.create({mode:"payment",line_items:[{quantity:qty,price_data:{currency:"usd",unit_amount:shop.priceCents,product_data:{name:GRADE_META[grade].name,description:shop.value}}}],success_url:`${base}/home/billing/success?session_id={CHECKOUT_SESSION_ID}`,cancel_url:`${base}/home/shop?billing=cancel`,customer_email:customerEmail,client_reference_id:livvUserId||undefined,metadata:{livv_kind:"pack",livv_grade:String(grade),livv_qty:String(qty),livv_username:usernameMeta,livv_user_id:livvUserId||""},allow_promotion_codes:true});return json({url:session.url,sessionId:session.id});}const tier=body.tier as PaidTier|undefined;if(!tier||!PAID.includes(tier))return json({error:"Invalid tier"},{status:400});const priceId=priceIdForTier(tier);if(!priceId)return json({error:`Missing STRIPE_PRICE_${tier.toUpperCase()} env var`},{status:503});const session=await stripe.checkout.sessions.create({mode:"subscription",line_items:[{price:priceId,quantity:1}],success_url:`${base}/home/billing/success?session_id={CHECKOUT_SESSION_ID}`,cancel_url:`${base}/home/profile?billing=cancel`,customer_email:customerEmail,client_reference_id:livvUserId||undefined,metadata:{livv_kind:"tier",livv_tier:tier,livv_username:usernameMeta,livv_user_id:livvUserId||""},subscription_data:{metadata:{livv_tier:tier,livv_username:usernameMeta,livv_user_id:livvUserId||""}},allow_promotion_codes:true});return json({url:session.url,sessionId:session.id});}catch(err){const message=err instanceof Error?err.message:"Checkout failed";console.error("[stripe/checkout]",message);return json({error:message},{status:500});}}