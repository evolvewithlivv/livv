"use client";
import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";
import { isSignedInLocal } from "@/lib/auth";
const LOGO="/livv-logo.png";
export default function OpeningPage(){const router=useRouter();const[ready,setReady]=useState(false);const[offline,setOffline]=useState(false);useEffect(()=>{let active=true;const update=()=>setOffline(!navigator.onLine);update();window.addEventListener("online",update);window.addEventListener("offline",update);(async()=>{try{if(isSignedInLocal()){router.replace("/home");return;}}finally{if(active)setReady(true);}})();return()=>{active=false;window.removeEventListener("online",update);window.removeEventListener("offline",update)}},[router]);if(!ready)return <main className="min-h-dvh bg-[#050505]"/>;return <main className="opening relative min-h-dvh overflow-hidden bg-[#050505] text-white"><div aria-hidden className="opening-atmosphere pointer-events-none absolute inset-0"/><div aria-hidden className="opening-veil pointer-events-none absolute inset-0"/><div className="relative z-10 flex min-h-dvh flex-col items-center justify-center px-6 pb-[max(2.5rem,env(safe-area-inset-bottom))] pt-16"><div className="opening-wordmark flex flex-col items-center"><img src={LOGO} alt="LIVV" className="h-16 w-16 object-contain drop-shadow-[0_0_40px_rgb(var(--livv-accent)/0.35)]"/></div><p className="opening-line mt-5 max-w-[18rem] text-center text-[15px] leading-snug text-white/55">Train. Read. Check in. Build a better day.</p><p className="opening-sub mt-3 text-[11px] uppercase tracking-[0.28em] text-white/25">Start simple. Stay consistent.</p>{offline&&<p role="status" className="mt-5 rounded-full border border-white/10 bg-white/[0.04] px-3 py-2 text-[11px] text-white/40">You’re offline. Reconnect to enter LIVV.</p>}<div className="opening-cta mt-12 flex w-full max-w-xs flex-col items-center gap-4"><button type="button" disabled={offline} onClick={()=>router.push("/auth")} className="group flex h-14 w-full items-center justify-center gap-2 rounded-full bg-white text-[15px] font-semibold text-black shadow-[0_12px_40px_rgba(0,0,0,0.45)] disabled:cursor-not-allowed disabled:opacity-40">Enter LIVV <span aria-hidden className="inline-block transition-transform duration-300 group-hover:translate-x-1">→</span></button><button type="button" onClick={()=>router.push("/auth")} className="text-[13px] text-white/40">Sign in</button><div className="mt-8 flex flex-wrap items-center justify-center gap-x-4 gap-y-2 text-[10px] uppercase tracking-[0.16em] text-white/25"><a href="/legal/privacy" className="hover:text-white/50">Privacy</a><a href="/legal/terms" className="hover:text-white/50">Terms</a><a href="/legal/community" className="hover:text-white/50">Community</a></div></div></div></main>}
