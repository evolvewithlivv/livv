"use client";
import { useEffect,useState } from "react";
const S=[["Reset",3,"Slow down and return to the next thing."],["Focus",5,"A short practice before work, study, or training."],["Night",10,"A quiet landing before sleep."]] as const;
const STEPS=["Settle your posture.","Breathe in slowly through your nose.","Exhale longer than you inhale.","Notice one thing you can control next."];
export default function MeditationPage(){const [idx,setIdx]=useState(0),[sec,setSec]=useState(180),[run,setRun]=useState(false),[done,setDone]=useState(0);const item=S[idx];useEffect(()=>{if(!run)return;const id=setInterval(()=>setSec(v=>{if(v<=1){setRun(false);setDone(n=>n+1);return 0}return v-1}),1000);return()=>clearInterval(id)},[run]);function choose(i:number){setIdx(i);setSec(S[i][1]*60);setRun(false)}const mm=String(Math.floor(sec/60)).padStart(2,"0"),ss=String(sec%60).padStart(2,"0");return <main className="livv-page min-h-full"><div className="mx-auto max-w-xl px-5 pb-14 pt-6"><header className="border-b border-livv-line pb-7 text-center"><a href="/home/health" className="text-[10px] font-semibold uppercase tracking-[.2em] text-livv-muted">Health / Mind</a><h1 className="mt-2 text-[clamp(1.8rem,8vw,2.5rem)] font-semibold tracking-[-.05em]">Take a minute.</h1><p className="mx-auto mt-3 max-w-[36ch] text-sm leading-6 text-livv-muted">Simple guided practices. No streak pressure.</p></header><div className="flex gap-2 overflow-x-auto py-7">{S.map((x,i)=><button key={x[0]} type="button" onClick={()=>choose(i)} className={"shrink-0 rounded-full border px-4 py-2.5 text-[10px] font-semibold uppercase tracking-[.13em] "+(idx===i?"border-livv-ink bg-livv-ink text-livv-bg":"border-livv-line text-livv-muted")}>{x[0]} · {x[1]}m</button>)}</div><section className="border-y border-livv-line py-10 text-center"><p className="text-[10px] uppercase tracking-[.18em] text-livv-muted">{item[2]}</p><div className="mt-5 text-[clamp(4rem,20vw,7rem)] font-semibold tracking-[-.08em] tabular-nums">{mm}:{ss}</div><div className="mt-6 flex justify-center gap-2"><button type="button" onClick={()=>setRun(v=>!v)} className="rounded-full bg-livv-ink px-7 py-3.5 text-[10px] font-semibold uppercase tracking-[.14em] text-livv-bg">{run?"Pause":sec===0?"Done":"Start"}</button><button type="button" onClick={()=>{setSec(item[1]*60);setRun(false)}} className="rounded-full border border-livv-line px-5 py-3.5 text-[10px] font-semibold uppercase tracking-[.14em] text-livv-muted">Reset</button></div><p className="mt-4 text-[10px] text-livv-muted">Completed: {done}</p></section><section className="py-7"><p className="text-[10px] font-semibold uppercase tracking-[.2em] text-livv-muted">Practice</p><div className="mt-3 divide-y divide-livv-line border-y border-livv-line">{STEPS.map((x,i)=><div key={x} className="flex gap-4 py-4"><span className="text-[10px] font-semibold text-livv-muted">{String(i+1).padStart(2,"0")}</span><p className="text-sm leading-6">{x}</p></div>)}</div></section></div></main>}
